# Inicializa o projeto
npm init -y

# Instala Express (API)
npm install express

# Instala body parsing e upload
npm install multer

# Conexão com ODBC (AS400)
npm install odbc

# Manipulação otimizada de imagens
npm install sharp

# Outras utilidades (opcional)
npm install dotenv cors
